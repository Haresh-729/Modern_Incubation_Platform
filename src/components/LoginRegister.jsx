import React from "react";

const LoginRegister = () => {
  return (
    <div className="h-screen flex items-center justify-center text-red-600 text-4xl">
      Please login or register to see the page
    </div>
  );
};

export default LoginRegister;
